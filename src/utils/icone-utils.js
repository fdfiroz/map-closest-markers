import { icon } from 'leaflet';

export const myIcon = icon({
    iconUrl: '/myMark.png',
    iconSize: [40, 45], // Optional: Set icon size
  });
export const otherIcon = icon({
    iconUrl: '/mark.png',
    iconSize: [15, 25], // Optional: Set icon size
  });